# Production Environment Variables Example
# Copy this file to .env and configure for your node
#
# IMPORTANT: Keep .env file secure and never commit it to git!

# =============================================================================
# Node Configuration
# =============================================================================

# Unique node identifier (used in container name and logs)
NODE_NAME=seed1

# Node role: "leader" or "follower"
NODE_ROLE=leader

# =============================================================================
# Temporary Consensus Configuration (REQUIRED FOR LEADER)
# =============================================================================

# Leader's X-CASH public address (used as leader ID)
# This MUST be one of the authorized seed node addresses:
#   NETWORK_DATA_NODE_PUBLIC_ADDRESS_1..4 (defined in cryptonote_config.h)
DELEGATE_PUBLIC_ADDRESS=

# DPoS Ed25519 keypair (64 bytes hex: [seed(32)][public(32)])
# Format: seed(32 bytes) + public_key(32 bytes) = 128 hex characters
# This is used for block signing via libsodium crypto_sign_detached()
# IMPORTANT: Keep this secret! Never commit to git!
DELEGATE_SECRET_KEY=

# =============================================================================
# Network Ports (MAINNET - Do not change)
# =============================================================================

P2P_BIND_PORT=18280
RPC_BIND_PORT=18281
ZMQ_RPC_BIND_PORT=18282

# =============================================================================
# Data Storage
# =============================================================================

# Path to blockchain data directory (LMDB)
# IMPORTANT: Should point to existing mainnet blockchain data
DATA_DIR=/var/lib/xcash

# Path to log files
LOG_DIR=/var/log/xcash

# =============================================================================
# Performance Tuning
# =============================================================================

# Maximum number of threads (default: 4, 0=auto)
MAX_CONCURRENCY=4

# Log verbosity (0=minimal, 4=debug)
LOG_LEVEL=0

# =============================================================================
# Example configurations for different nodes
# =============================================================================

# --- SEED1 (Leader) ---
# NODE_NAME=seed1
# NODE_ROLE=leader
# DELEGATE_PUBLIC_ADDRESS=<your_seed1_address>
# DELEGATE_SECRET_KEY=<your_seed1_secret_key>

# --- SEED2 (Follower) ---
# NODE_NAME=seed2
# NODE_ROLE=follower
# DELEGATE_PUBLIC_ADDRESS=
# DELEGATE_SECRET_KEY=

# --- SEED3 (Follower) ---
# NODE_NAME=seed3
# NODE_ROLE=follower
# DELEGATE_PUBLIC_ADDRESS=
# DELEGATE_SECRET_KEY=

# --- SEED4 (Follower) ---
# NODE_NAME=seed4
# NODE_ROLE=follower
# DELEGATE_PUBLIC_ADDRESS=
# DELEGATE_SECRET_KEY=
